---
extends: stylelint-config-standard
rules:
  at-rule-no-unknown:
  - true
  - ignoreAtRules:
    - extend
    - mixin
    - if
    - content
    - include
    - else
    - return
    - function
  at-rule-empty-line-before: null
